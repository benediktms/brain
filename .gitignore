/target
.omc
.claude

.DS_Store

.env

# bv (beads viewer) local config and caches
.bv/

# Dolt database files (added by bd init)
.dolt/
*.db

*.log

# Generated by `brain docs` — contains per-machine instructions, fully rebuildable
AGENTS.md

# Brain project state — local-only (brain id, task events, auto-inject config).
# Cross-machine attach is handled by `brain init`'s git common-dir lookup
# (Case C); the marker file is no longer needed in version control.
.brain/
