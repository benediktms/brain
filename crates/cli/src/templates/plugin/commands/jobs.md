---
description: Check async job queue status
---

Display the status of brain's async job queue.

Use the `jobs_status` tool to get:
- Queue health metrics
- Pending, running, and completed job counts
- Failed jobs and error details

The job queue handles async operations like LLM-powered summarization. Use this to check if background work is progressing or stuck.
